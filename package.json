{
	"name": "puke-debug",
	"displayName": "Puke-Debug",
	"description": "Inserts simple, emetic debug lines where you need them. Last resort option in debugging...",
	"version": "0.1.0",
	"engines": {
		"vscode": "^1.33.1"
	},
	"license": "SEE LICENSE IN LICENSE",
	"publisher": "Zorvalt",
	"repository": "https://github.com/Zorvalt/Puke-Debug",
	"keywords": [
		"debug",
		"print",
		"log",
		"echo",
		"line",
		"useless",
		"fun",
		"vomit",
		"puke"
    ],
	"categories": [
		"Other"
	],
	"activationEvents": [
		"*"
	],
	"main": "./out/extension.js",
	"contributes": {
		"configuration": {
			"type": "object",
			"title": "Puke-Debug",
			"properties": {
				"puke-debug.pukePointFormat": {
					"type": "string",
					"default": "fmt.Println('PUKE-POINT: filename: %filename%, line: %line%')",
					"description": "Puke-Point format. Use %line% and %filename% as tags for debug info."
				},
				"puke-debug.commentOpening": {
					"type": "string",
					"default": "//",
					"description": "The string to open a comment"
				},
				"puke-debug.commentTAG": {
					"type": "string",
					"default": "PKDBG",
					"description": "The comment tag to identify puke-points"
				},
				"puke-debug.commentClosing": {
					"type": "string",
					"default": "",
					"description": "The string to close a comment"
				},
				"puke-debug.updateOnSave": {
					"type": "boolean",
					"default": "true",
					"description": "Updates the Puke-Points on save"
				}
			}
		},
		"commands": [
			{
				"command": "pukeDebug.insertPukePoint",
				"title": "Insert puke-point"
			},
			{
				"command": "pukeDebug.clearPukePoints",
				"title": "Clears all puke-points in current file"
			},
			{
				"command": "pukeDebug.updatePukePoints",
				"title": "Updates all puke-points in current file"
			}
		],
		"keybindings": [
			{
				"command": "pukeDebug.insertPukePoint",
				"key": "ctrl+insert",
				"mac": "cmd+insert"
			}
		]
	},
	"scripts": {
		"vscode:prepublish": "npm run compile",
		"compile": "tsc -p ./",
		"watch": "tsc -watch -p ./",
		"pretest": "npm run compile",
		"test": "node ./out/test/runTest.js"
	},
	"devDependencies": {
		"@types/glob": "^7.1.1",
		"@types/mocha": "^5.2.6",
		"@types/node": "^10.12.21",
		"@types/vscode": "^1.38.0",
		"glob": "^7.1.4",
		"mocha": "^6.1.4",
		"typescript": "^3.3.1",
		"tslint": "^5.12.1",
		"vscode-test": "^1.0.2"
	}
}
